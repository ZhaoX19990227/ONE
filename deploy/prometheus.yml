global:
  scrape_interval: 20s
  evaluation_interval: 20s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: one-server
    metrics_path: /api/actuator/prometheus
    static_configs:
      - targets: [server:8080]
